version: "3.8"

services:
  csms:
    build:
      context: ./csms
      dockerfile: Dockerfile
    container_name: csms
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO", "-", "http://localhost:8080/health"]
      interval: 5s
      timeout: 3s
      retries: 5

